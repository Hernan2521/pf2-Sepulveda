import { FiltrarEditarPipe } from './filtrar-editar.pipe';



describe('FiltrarEditarPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarEditarPipe();
    expect(pipe).toBeTruthy();
  });
});
